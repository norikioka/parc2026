# Colab用の追加依存関係。
#
# LeRobot本体・π0.5・LIBERO/LIBERO-Plusのインストール手順は requirements.txt では管理しない。
# 手順が複雑（extras指定・CUDAバージョン整合・HF認証・アセット別配布）なため、
# notebooks/colab_bootstrap.ipynb の該当セル、または docs/env_setup.md を参照して
# `git clone` + `uv pip install -e ".[pi,libero]"` 方式で構築すること。
#
# ここにはそれ以外の補助ツールのみ書く。

wandb   # 学習ログ・自分のスコア推移の記録用(任意)
