# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Notebook checkpoints
.ipynb_checkpoints/

# データ・学習成果物(Colab側のGoogle Driveで管理するためgit管理外)
data/
checkpoints/
*.pt
*.pth
*.ckpt
*.safetensors
wandb/

# 学習実験の成果物ディレクトリ(モデル重みが数百MB〜GB単位になるためgit管理外)
# 比較CSV等の小さい結果ファイルだけは個別にgit add -f で追跡してよい
1st/
2nd/
3rd/
experiments/


# 提出用zip(数百MB、モデル重み込みのためgit管理外。再現はvendoringされたコード+1st/のモデルから可能)
submission.zip
submission_*.zip

# macOS
.DS_Store
